import { Completion } from '../interface';

// practical utility

export default [
  {
    label: '@{date.formatNow()}',
    detail: 'get yyyy-MM-dd HH:mm:ss format date string',
    section: {
      name: 'utility',
    },
  },
] as Completion[];
